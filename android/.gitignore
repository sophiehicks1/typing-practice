# Gradle / Android build outputs and local machine config.
.gradle/
build/
app/build/
local.properties
*.apk
*.keystore

# The web build copied in at build time (generated, not source).
app/src/main/assets/index.html
